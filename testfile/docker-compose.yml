version: '3.9'

services:
  app:
    build:
      context: .
    ports:
      - 5000:5000
    volumes:
      - .:/app
    depends_on:
      - selenium
  selenium:
    image: selenium/standalone-chrome
    shm_size: 2g
    ports:
      - 4444:4444
    restart: always